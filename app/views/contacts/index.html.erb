<br>
<%= link_to new_contact_path, class: 'btn btn-primary', class: 'btn btn-primary' do %>
    <i class="glyphicon glyphicon-plus"></i> New Contact
<% end %>

<style type="text/css">
.table th
{
  text-align: center;
}
</style>

<% if cannot? :manage, :all %>
  <%= render 'current' %>
<% else %>
  <%= render 'all' %>
<% end %>